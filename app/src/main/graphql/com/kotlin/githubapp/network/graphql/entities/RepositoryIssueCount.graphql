query RepositoryIssueCount($repo: String!, $owner: String!){
  repository(name: $repo, owner: $owner) {
    openIssues: issues(first:10){
      totalCount
    }
    closedIssues: issues{
      totalCount
    }
  }
}